# Minimal dependencies for the package and its CI test suite.
# This file is consumed by `pip install -r requirements.txt` (e.g. in CI),
# so it must contain only valid PEP 508 requirement lines.
#
# For the FULL environment needed to run the model (mpich, mpi4py, toml,
# xarray, dask, zarr<3, and optional jax / plotting tools), see INSTALL.md.
numpy>=1.24.0
